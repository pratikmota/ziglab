---
id: memory/try-optional
chapter: memory
slug: try-optional
type: try
title: Unwrap safely
description: Use orelse when an optional is null.
order: 3
level: advanced
videoUrl: ""
docsUrl: "#Optionals"
starterFile: try-optional.zig
expectedOutput: "4"
hints:
  - maybe is null. It is not 0.
  - orelse 4 runs because the optional is missing.
  - Change maybe to a number if you want. Keep null if you still want 4.
---

The starter has a `?i32` that is `null`. `orelse` fills in `4`.

Run. You should see `4`. That is the missing case, handled on the page — not a crash, not a silent zero unless you chose zero as the fallback.
