---
id: comptime/try-comptime
chapter: comptime
slug: try-comptime
type: try
title: A tiny comptime function
description: Make a comptime function return three times its argument.
order: 3
level: advanced
videoUrl: ""
docsUrl: "#comptime"
starterFile: try-comptime.zig
expectedOutput: "15"
hints:
  - "triple's argument is comptime n. 5 is a literal, so it qualifies."
  - "The body currently returns n. Return n * 3. 5 * 3 is 15."
  - "A runtime var would not be allowed as that argument."
---

The starter’s `triple` takes a `comptime` integer. `main` calls `triple(5)`. Right now it returns `n` unchanged, so you would see `5`.

Return `n * 3`. The math can happen while compiling because the input is already known.
