---
id: functions/try-function
chapter: functions
slug: try-function
type: try
title: Write an add function
description: Fill in add so it returns the sum of its two arguments.
order: 2
level: core
videoUrl: ""
docsUrl: "#Functions"
starterFile: try-function.zig
expectedOutput: "5"
hints:
  - add takes two i32 arguments. Return their sum.
  - "main calls add(2, 3) and prints with {d}."
  - Replace return a with return a + b. You can drop _ = b once b is used.
---

The starter defines `add` but currently returns only `a`, so the program prints `2`.

Return `a + b`. Leave `add(2, 3)` if you want `5`.
