---
id: hello-world/print-text
chapter: hello-world
slug: print-text
type: try
title: Print text
description: Change the Hello World message in the editor and keep the print call in view.
order: 2
level: start
videoUrl: ""
docsUrl: ""
starterFile: print-text.zig
expectedOutput: Hello, ZigLab
hints:
  - The first argument to print is the message. The quotes hold the text that will appear.
  - Change the words inside the quotes. Keep the \\n if you still want a newline.
  - Run to see the preview. Until the in-browser compiler is connected, only the starter (and Hello World) show this expected line.
---

The editor on this page starts with the tiny program from the previous lesson. Your job is to **change the message**.

Keep `std.debug.print`. Keep the empty tuple `.{ }` if you are not inserting values. Edit the string. The draft stays in this browser if you refresh.

Until a real in-browser compiler is wired up, Run is a preview: the starter output is shown below. Changing the text is still the exercise — you will see the live result when WASM lands.
