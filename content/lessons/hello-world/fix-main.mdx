---
id: hello-world/fix-main
chapter: hello-world
slug: fix-main
type: challenge
title: Make it run
description: Repair a broken Hello World so the entry point is public.
order: 3
level: start
videoUrl: ""
docsUrl: "#Hello-World"
starterFile: fix-main.zig
expectedOutput: "Hello, ZigLab"
hints:
  - Zig looks for a public function named main as the program entry point.
  - A function without pub is private to this file. The runtime cannot call it.
  - Change fn main to pub fn main, then Run. The message can stay Hello, ZigLab.
---

This starter looks almost like Hello World, but it will not start. Something about `main` is missing.

**The compiler is the teacher.** When Run fails, read the first error. It is pointing at a name the runtime cannot see. Fix that name — do not invent a new program.

A function without `pub` stays private to this file. The entry point has to be visible.

Show a hint if you want a nudge. One hint at a time.
