---
id: values/challenge-compare
chapter: values
slug: challenge-compare
type: challenge
title: Compare and print
description: Bind a bool from a comparison and print it.
order: 5
level: start
videoUrl: ""
docsUrl: "#Operators"
starterFile: challenge-compare.zig
expectedOutput: "true"
hints:
  - "n is already 12. Compare it with >= 10. That comparison is a bool."
  - "Replace n < 10 with n >= 10. You do not need if yet."
  - "Print already uses {}. Bools show as true or false."
---

The starter binds `n` and a bool named `at_least_ten` from a comparison that is currently **wrong**.

`n` is **at least 10**. Fix the comparison on the right-hand side of `at_least_ten`, then Run. The print is already in place.
